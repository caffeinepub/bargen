{
  "kind": "build_request",
  "title": "Publish the current app to live",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Publish the currently deployed draft of the application to a live (non-expiring) deployment so it remains accessible without requiring a rebuild.",
      "target": "unknown",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "publish it"
        ]
      },
      "acceptanceCriteria": [
        "A live deployment is created from the current project state (not a draft-only deployment).",
        "The published app is reachable via its live URL and loads the main route ('/').",
        "Backend canister calls function as they did in the draft deployment (no regressions introduced by publishing)."
      ]
    }
  ],
  "constraints": [
    "Use English for any user-facing text."
  ],
  "nonGoals": [
    "Implementing new application features or changing existing UI/UX beyond what is required to publish."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}