{
  "kind": "build_request",
  "title": "Publish current Bargen draft as a live shareable version",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-4",
      "text": "Publish the currently working Bargen draft (Version 4) as a live (non-draft) deployment so the user can share a stable public link with others to open in a browser.",
      "target": "unknown",
      "source": {
        "messageIds": [
          "msg-20"
        ],
        "quotes": [
          "pk publish it"
        ]
      },
      "acceptanceCriteria": [
        "A new live (non-draft) version is created from the current Version 4 state without requiring additional user input.",
        "A live/shareable URL is produced for the newly published version (distinct from the draft preview link).",
        "The published live version loads successfully and matches the current draft functionality (product browsing, cart, bargaining, messaging, and INR price display)."
      ]
    }
  ],
  "constraints": [
    "Do not modify files in frontend immutablePaths: frontend/src/hooks/useInternetIdentity.ts, frontend/src/hooks/useInternetIdentity.tsx, frontend/src/hooks/useActor.ts, frontend/src/main.tsx, frontend/src/components/ui.",
    "Do not introduce additional backend canisters or services; keep the single-actor Motoko architecture."
  ],
  "nonGoals": [
    "Adding sample products or changing product visibility/seed data.",
    "Implementing new features (e.g., dual USD+INR display, remove-from-cart, performance optimizations).",
    "Backend state migrations or schema changes unless strictly required for publishing."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}